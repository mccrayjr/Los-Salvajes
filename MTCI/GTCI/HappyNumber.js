const findHappyNumber = (num) => {
  let slow = num,
  fast = num;
  
  while(true){
    slow = //one fun rund
    fast = //func runs twice
    if(slow === fast){
      break;
    }
  }
  return slow === 1
}

const digitSquarinator = (num) => {
  
}